{
  "name": "eteria-rpg-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run generate:hero && vite --host 0.0.0.0",
    "build": "npm run generate:hero && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "android:add": "cap add android",
    "android:sync": "npm run build && cap sync android",
    "android:open": "cap open android",
    "generate:hero": "node scripts/generate-hero-gltf.mjs"
  },
  "dependencies": {
    "@capacitor/core": "8.5.0",
    "three": "0.185.1"
  },
  "devDependencies": {
    "@capacitor/android": "8.5.0",
    "@capacitor/cli": "8.5.0",
    "vite": "8.2.1"
  }
}
